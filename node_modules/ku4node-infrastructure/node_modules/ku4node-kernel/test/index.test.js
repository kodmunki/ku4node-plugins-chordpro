var $kernel = require('../lib/index');

exports['modules'] = function (test) {
    test.expect(23);
    test.ok($kernel.asserters);
    test.ok($kernel.lock);
    test.ok($kernel.math);
    test.ok($kernel.obj);
    test.ok($kernel.replicate);
    test.ok($kernel.str);
    test.ok($kernel.uid);
    test.ok($kernel.class);
    test.ok($kernel.collections.hash);
    test.ok($kernel.collections.list);
    test.ok($kernel.dateTime.dayPoint);
    test.ok($kernel.finance.money);
    test.ok($kernel.geometry.coord);
    test.ok($kernel.geometry.vector);
    test.ok($kernel.geometry.rectangle);
    test.ok($kernel.patterns.abstractContext);
    test.ok($kernel.patterns.abstractState);
    test.ok($kernel.patterns.abstractVisitor);
    test.ok($kernel.patterns.iterator);
    test.ok($kernel.patterns.mediator);
    test.ok($kernel.patterns.observer);
    test.ok($kernel.patterns.rolodex);
    //test.ok(kernel.patterns.sorter);
    test.ok($kernel.patterns.specification);
    test.done();
};