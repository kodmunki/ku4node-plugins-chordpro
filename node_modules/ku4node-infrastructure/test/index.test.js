var $infrastrucure = require('../lib/index');

exports['modules'] = function (test) {
    test.expect(3);
    test.ok($infrastrucure.data.queryString);
    test.ok($infrastrucure.data.json);
    test.ok($infrastrucure.data.dto);
    test.done();
};